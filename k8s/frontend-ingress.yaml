apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: app-4-dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: angular.138.2.131.203.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-service
                port:
                  number: 80
